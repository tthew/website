import { colors } from "./colors";
import { breakpoint, mediaQueries } from "./mediaQueries";

export const base = {
  primary100: colors.tombstoneGrey100,
  primary90: colors.tombstoneGrey90,
  primary80: colors.tombstoneGrey80,
  primary70: colors.tombstoneGrey70,
  primary60: colors.tombstoneGrey60,
  primary50: colors.tombstoneGrey50,
  primary40: colors.tombstoneGrey40,
  primary30: colors.tombstoneGrey30,
  primary20: colors.tombstoneGrey20,
  primary10: colors.tombstoneGrey10,
  uiBlack: colors.tombstoneGrey105,
  // uiBlack: `#08090d`,
  uiWhite: `#f5f5f5`
};

export { colors, mediaQueries };

export default base;
